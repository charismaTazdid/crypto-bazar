import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    title: {
        flex: 1,
        color: "orange",
        fontFamily: 'Montserrat',
        fontWeight: 'bold',
        cursor: 'pointer'
    }
}));
